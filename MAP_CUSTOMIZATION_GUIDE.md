# دليل تعديل الخريطة - Map Customization Guide

هذا الدليل يوضح أين يمكن تعديل أحجام وألوان عناصر الخريطة.

## 🅿️ 1. حجم أيقونة P للموقف (Parking Marker)

**الملف:** `lib/features/parking_map/presentation/pages/parking_map_screen.dart`

**السطر:** حوالي 182

```dart
await _mapController!.addMarker(
  geoPoint,
  markerIcon: MarkerIcon(
    icon: Icon(
      Icons.local_parking,
      color: isSelected ? AppColors.primary : AppColors.primary.withValues(alpha: 0.8),
      size: isSelected ? 56 : 52, // ⬅️ عدل هنا لتغيير الحجم
    ),
  ),
);
```

**ملاحظة:** 
- الحجم الحالي: `52` (غير محدد) و `56` (محدد)
- يمكن زيادته إلى `60/64` للحجم الأكبر
- يمكن تقليله إلى `48/52` للحجم الأصغر

---

## 📍 2. حجم سهم الموقع الحالي (Direction Arrow)

**الملف:** `lib/features/parking_map/presentation/pages/parking_map_screen.dart`

**السطر:** حوالي 370-375

```dart
directionArrowMarker: MarkerIcon(
  icon: Icon(
    Icons.navigation,
    color: AppColors.primary, // لون التطبيق
    size: 48, // ⬅️ عدل هنا لتغيير الحجم (كان 32)
  ),
),
```

**ملاحظة:** 
- الحجم الحالي: `48`
- يمكن زيادته إلى `56` أو `64` للحجم الأكبر
- يمكن تقليله إلى `40` للحجم الأصغر

---

## 📌 3. حجم الـ Pin الحمراء عند الموقف (Destination Pin)

**الملف:** `lib/features/parking_map/presentation/widgets/parking_details_bottom_sheet.dart`

**السطر:** حوالي 290-300

```dart
await widget.mapController!.addMarker(
  parkingGeoPoint,
  markerIcon: MarkerIcon(
    icon: Icon(
      Icons.location_on,
      color: Colors.red,
      size: 72, // ⬅️ عدل هنا لتغيير الحجم
    ),
  ),
);
```

**ملاحظة:**
- الحجم الحالي: `72`
- يمكن زيادته إلى `80` أو `88` للحجم الأكبر
- يمكن تقليله إلى `64` للحجم الأصغر

---

## 🛣️ 4. حجم وسماكة خط الاتجاهات (Route Line)

**الملف:** `lib/features/parking_map/presentation/widgets/parking_details_bottom_sheet.dart`

**السطر:** حوالي 262-266

```dart
roadOption: RoadOption(
  roadColor: AppColors.primary, // لون التطبيق
  roadWidth: 16, // ⬅️ عدل هنا لتغيير السماكة (كان 12)
  zoomInto: true,
),
```

**ملاحظة:**
- السماكة الحالية: `16`
- يمكن زيادتها إلى `18` أو `20` للخط الأسمك
- يمكن تقليلها إلى `14` للخط الأرفع
- القيم الموصى بها: بين `12` و `20`

---

## 📝 ملخص التعديلات السريعة

| العنصر | الملف | السطر | المعامل | القيمة الحالية |
|--------|-------|-------|---------|----------------|
| **أيقونة P (الموقف)** | `parking_map_screen.dart` | ~182 | `size` | `52/56` |
| **سهم الموقع** | `parking_map_screen.dart` | ~374 | `size` | `60` |
| **Pin الوجهة** | `parking_details_bottom_sheet.dart` | ~299 | `size` | `80` |
| **خط الاتجاهات** | `parking_details_bottom_sheet.dart` | ~264 | `roadWidth` | `28` |

---

## 🎨 نصائح التصميم

- **أيقونة P (الموقف):** حجم `52-56` مناسب للوضوح
- **سهم الموقع:** حجم `56-64` مناسب للوضوح
- **Pin الوجهة:** حجم `72-88` يجعلها واضحة ومميزة
- **خط الاتجاهات:** سماكة `20-28` توازن بين الوضوح والأناقة

---

## ⚙️ إعدادات إضافية

### إلغاء Pin الحمراء عند فتح الخريطة

**الملف:** `lib/features/parking_map/presentation/pages/parking_map_screen.dart`

**السطر:** حوالي 363-368

```dart
personMarker: MarkerIcon(
  icon: Icon(
    Icons.location_on,
    color: Colors.transparent, // شفاف - لا يظهر pin
    size: 0, // حجم صفر
  ),
),
```

### إلغاء التتبع التلقائي للموقع

**الملف:** `lib/features/parking_map/presentation/pages/parking_map_screen.dart`

**السطر:** حوالي 366-369

```dart
userTrackingOption: const UserTrackingOption(
  enableTracking: false, // تعطيل التتبع التلقائي
  unFollowUser: true, // لا يتابع الموقع تلقائياً
),
```

**ملاحظة:** الآن الخريطة لن تمركز تلقائياً على موقعك. فقط عند الضغط على زر "My Location" في AppBar.

---

## ⚠️ ملاحظات مهمة

1. بعد أي تعديل، احفظ الملف وأعد تشغيل التطبيق
2. تأكد من أن القيم موجبة (أكبر من 0)
3. القيم الكبيرة جداً قد تؤثر على الأداء

